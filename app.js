function sum(sum1, sum2){
  var sum = sum1 + sum2;
  console.log('The sum of ' + sum1 + ' + ' + sum2 + ' = ' + sum + '.');
  return sum;
}

function multi(mult1,multi2){
  var multi = multi1 * multi2;
  console.log('The product of ' + mutli1 + ' and ' + multi2 + ' is ' + multi);
  return multi;
}
